import { UniversalFormatPipe } from './universal-format-pipe';

describe('UniversalFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new UniversalFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
